import 'package:flutter/material.dart';

class OverviewLocationForecast extends StatelessWidget {
  const OverviewLocationForecast({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
